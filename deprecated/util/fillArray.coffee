###*
 * Fill an array with a particular value
 * @param {*} fill
 * @param {Integer} length
 * @return {[]}
 ###
fillArray = (fill, length) ->
    while length--
        fill

module.exports = fillArray